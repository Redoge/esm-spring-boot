spring:
  jpa:
    properties:
      hibernate:
        connection:
          isolation: 4
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: